@model RoleViewModel

<div class="row accountForm col-sm-6 col-sm-offset-3">
    <form asp-controller="Role" asp-action="Change" method="post">
    
        <h2>Change</h2>


        <select name="selecteduser" onchange="this.form.submit()" >
            @foreach (User user in Model.Users)
            {


                if (Model.selectedUserId == user.Id)
                {
                    <option selected value="@user.Id">
                        @user.Firstname
                    </option>
                }

                else
                {
                    <option value="@user.Id">
                        @user.Firstname
                    </option>

                }
            }
        </select>


        <select name="selectedrole">
            @foreach (Role role in Model.Roles)
            {

                if (Model.SelectedRole == null)
                {

                    if (Model.SelectedRole.id == role.id)
                    {
                    <option selected value="@role.id">
                        @role.Name
                    </option>
                    }


                    else
                    {
                    <option value="@role.id">
                        @role.Name
                    </option>
                    }
                }

            }

        </select>

        <input type="submit" value="BEVESTIGEN" />
    
        </form>

</div>
